# Hints for Task 2

<figure><img src="../../../.gitbook/assets/image (199).png" alt=""><figcaption></figcaption></figure>

1. Same playbook opening as we have used in the previous examples
2. Gather IOS facts from the devices
3. Call your module as you would any other module. As long as the name matches (without the .py extension) and it is located in the library folder, it will be found automatically
4. Indented under the module you put the input variables
5. Write the results to a YAML file using the builtin copy module
6. Try to finish the line yourself (or look at next page)
7. Try writing the results to JSON, it should be pretty similar, just format the content using "to\_nice\_json" instead

