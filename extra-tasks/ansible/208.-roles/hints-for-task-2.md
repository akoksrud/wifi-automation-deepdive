# Hints for Task 2

To "create" the role get\_version we have created the subfolder `get_version` with another subfolder `tasks`, in where there is a file named `main.yml`. This structure is identical for all roles, just change the upper folder name

As you can see from the example below, the role itself does just contain the same tasks that we create in [115.-exercise-gather-facts.md](../../../main-tasks/ansible/115.-exercise-gather-facts.md "mention"), except that we output the results to the terminal using debug, instead of writing to a file

<div data-full-width="true"><figure><img src="../../../.gitbook/assets/image (3) (1) (1) (1) (1) (1) (1).png" alt=""><figcaption></figcaption></figure></div>
